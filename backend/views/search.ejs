<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <% include ./header %>
    <title>분실물 토탈 관리</title>
    <link rel="stylesheet" href="/stylesheets/search_style.css">
</head>
<body>

  <!-- 제목 물결 박스 -->
  <div class='box'>
    <div class='wave -one'></div>
    <div class='wave -two'></div>
    <div class='wave -three'></div>
    <div class='title'>분실물 <br>토탈<br>관리</div>
  </div>

  <div class="container">
    <div class="row">
      <div class="col-lg-12" style="margin-bottom: 40px">
        <h1 class="main-title" onclick="location.href='./'" style="cursor:pointer">분실물 토탈 검색 결과</h1>
      </div>
    </div>
    <div class="row condition-row" style="padding:0 10%">
      <div class="col-lg-12">
        <div class="row condition-row" style="text-align:center; margin: 20 0px;">
          <div class="col-6 col-md-3">
            <strong>🗓</strong> <span class="font-size-temp"><%=condition.date%></span>
          </div>
          <div class="col-6 col-md-3">
            <strong>🌏</strong><span class="font-size-temp"><%=condition.location%></span>
          </div>
          <div class="col-6 col-md-3">
            <strong>📚</strong><span class="font-size-temp"><%=condition.category%></span>
          </div>
          <div class="col-6 col-md-3">
            <strong>🔎</strong> <span class="font-size-temp"><%=condition.name%></span>
          </div>
        </div>
        <form method='post' action='./register'>
          <input type='hidden' name='date' value='<%=condition.date%>'/>
          <input type='hidden' name='location' value='<%=condition.location%>'/>
          <input type='hidden' name='category' value='<%=condition.category%>'/>
          <input type='hidden' name='name' value='<%=condition.name%>'/>
          <div class="row" style="text-align:center">
            <div class="col-6 offset-3" style="border-width:1px" onclick='this.form.submit()'>
                찾으시는 물건이 없으신가요? 알림 추가하기!
            </div>
          </div>
        </form>
        <div class="row">
          <div class="col-lg-12">
            <table class='table table-striped'>
              <thead>
                <tr>
                  <th>내용</th>
                  <th>분류</th>
                  <th>습득위치</th>
                  <th>습득일자</th>
                  <th>현재보관위치</th>
                  <th>전화번호</th>
                </tr>
              </thead>
              <%
              if(!items){items = [];}
              items.forEach(function(item){ %>
                <tr class="<%=item.insert_time>last_query_date?'table-danger':''%>">
                  <td><%=item.name%></td>
                  <td><%=item.category%></td>
                  <td><%=item.take_place%></td>
                  <td><%=item.date%></td>
                  <td><%=item.keep_place%></td>
                  <td><%=item.contact%></td>
                </tr>
              <% }); %>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>

</body>
</html>
